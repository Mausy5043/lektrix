# for pip

# pip can't install the dependencies from pyproject.toml
# when changing this file also consider `pyproject.toml` and `environment.yml`

gracefulkiller~=0.4
matplotlib~=3.10
mausy5043-common==2.11.6

# already delivered by mausy5043-common, so no need to specify version here:
numpy
pandas
py-solaredge==0.0.4.9
# pyarrow v21.0.0 crashes with "Illegal Instruction"
pyarrow
python-dateutil~=2.9
#
python-homewizard-energy==10.2.0
pytz
requests
sessypy==0.2.6
sh~=2.2
tabulate~=0.10
zeroconf==0.150.0
