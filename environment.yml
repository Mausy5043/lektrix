# When making changes to this file update requirements.txt also !!
# Usage: conda -v env update -n lektrix --file environment.yml

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.9
  - matplotlib
  - libtiff
  - numpy
  - pandas
  - pip
# This won't install on anything other than Linux (notably Raspberry Pi):
# - pyserial=3.5
  - python-dateutil
  - pytz
  - requests
  - scipy
  # Not on conda channels:
  - pip:
      - mausy5043-common

